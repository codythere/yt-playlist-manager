###############################################
# Playlist Manager - Environment Variables #
###############################################

#===============================================================================
# App Environment
#===============================================================================

# Node environment: development | production
NODE_ENV=development

# Your public-facing base URL (used for OAuth redirect, emails, links, etc.)
# ⚠ In production, replace with real domain, e.g. https://yourapp.com
APP_BASE_URL=http://localhost:3000

# Next.js public base URL (exposed to browser)
# ⚠ Should match the domain used for frontend requests
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Session signing key for auth state / cookies
# ⚠ MUST be replaced with a long random string in production
SESSION_SECRET=dev-super-secret-change-me


# Database Configuration
# Example (PostgreSQL (Docker) / Neon):
# postgres://postgres:postgres@localhost:5432/ytpm  
# DATABASE_URL=postgresql://neondb____
DATABASE_URL=


# Google OAuth Credentials
# OAuth Client ID / Secret from Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Redirect URI must match Google OAuth config
# ⚠ Change if deployed: https://yourapp.com/api/auth/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback


# Quota & API Behavior
# Daily quota limit (estimate of YouTube API quota usage allowance)
# Expiration date of the additional quota: 2026/05/21
# After the expiration date, the quota will revert back to 10,000.
YTPM_DAILY_QUOTA=100000 

# Logging verbosity: debug | info | warn | error
LOG_LEVEL=debug

# QUOTA_MODE: global | perUser
QUOTA_MODE=global
